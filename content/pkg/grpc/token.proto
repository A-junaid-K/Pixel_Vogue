syntax="proto3";

package grpc;

option go_package="/grpc";

service TokenService{
    rpc VerifyToken(tokenRequest) returns(tokenResponse);
}

message tokenRequest{
    string contributor_token=1;
}

message tokenResponse{}